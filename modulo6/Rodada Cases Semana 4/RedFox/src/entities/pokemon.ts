export class Pokemon {
    constructor(
        id: string,
        name: string,
        pokedex_number: number,
        img_name: string,
        generation: number,
        evolution_stage:string,
    ){}
}

// Devido à quantidade ABSURDA de info na tabela, reduzi as que são informadas, assim como as que foram upadas para o MYSQL. Tava demorando DEMAIS.